# AGENTS.md — 달빛 윷놀이 개발 노트

이 문서는 지금까지 작업한 내용과 다음에 이어서 패치하면 좋을 항목을 정리한 것입니다. 사용자 대상 설명은 [README.md](README.md)를 참고하세요. 이 문서는 다음에 이 프로젝트를 건드릴 사람(또는 AI 에이전트)을 위한 것입니다.

## 프로젝트 구조

순수 프론트엔드(백엔드 없음, `python -m http.server`로 정적 서빙)입니다.

- [index.html](index.html) — 홈 / 던지기(capture) / 결과(result) / 종료(ended) 4개 섹션을 `hidden` 속성으로 전환하는 단일 페이지. 말판 패널(`#boardPanel`)은 섹션 밖에 두고 JS로 표시 여부만 토글합니다.
- [app.js](app.js) — 전체 상태 머신, 카메라/MediaPipe 연동, DOM 렌더링. 다른 모듈의 순수 로직을 가져다 DOM에 반영하는 역할만 합니다.
- [motion.js](motion.js) — 카메라 프레임에서 팔/몸통 동작을 분석해 세기·판정을 계산하는 순수 로직 (DOM 없음).
- [board.js](board.js) — 말판 좌표, 말 이동/합치기/잡기/이벤트 로직 (DOM 없음, `app.js`가 렌더링만 담당).
- [style.css](style.css) — 한 파일에 전부. 기존 코드가 한 줄로 압축된 스타일이 많아 새 규칙도 대체로 그 스타일을 따랐습니다.
- `*_test.py` — Playwright 기반 스모크 테스트. `python -m http.server 8000`이 떠 있어야 동작합니다. **주의: 팀/말판 기능 추가로 화면 흐름이 바뀌어서 현재 테스트들이 깨져 있을 가능성이 높습니다** (예: `returnSeconds` 기본값이 3→10초로 바뀜, 결과 화면에서 "다음 던지기" 버튼이 말 이동 완료 전에는 숨겨짐 등). 실행/보수 필요.

## 지금까지 구현한 기능 (시간순)

1. **팀 등록** (`app.js`의 `teams`, `localStorage: yut-teams-v1`) — 홈에서 이름으로 팀 추가/삭제(최대 8팀), 팀별 점수 필드.
2. **점수판 다이얼로그** — 진행 중 `#scoreDialog`로 점수 입력/±1 조정. 입력 중 포커스가 빠지지 않도록 다이얼로그 전체를 다시 그리지 않고 `renderTeamList()`만 갱신하는 점 주의 (`setScore` 함수 주석 참고).
3. **결과 화면 자동 복귀 시간 기본값 10초** + **차례 넘기기 버튼** (`passTurn()`) — 대기 없이 바로 다음 팀으로.
4. **카메라 뷰어 레이아웃 변경** — 던지기 화면에서 카메라를 상단에 크게(`min-height:78vh`), 안내/세기 패널은 그 아래 `aside`에 가로 배치.
5. **말판 (윷판)** — 바깥 둘레 20칸(`board.js`의 `NODES`)을 도는 전통 윷판. 모서리(0/5/10/15)는 굵은 원, 대각선 지름길은 시각적 장식만(`DIAGONALS`) — **실제 이동 로직은 지름길을 타지 않고 20칸을 전부 돕니다.**
6. **말 이동 시스템** (`board.js`의 `applyMove`) — 도/개/걸/윷/모 결과에 따라 1~5칸 이동. 같은 팀 말은 한 칸에서 스택으로 합쳐지고 함께 이동, 상대 팀 말이 있는 칸에 도착하면 그 스택 전체를 대기 상태로 돌려보내고(잡기) 보너스 턴 부여. 윷·모도 보너스 턴. 20칸을 넘기면 완주(정확히 도착하지 않아도 됨 — 정통 규칙과 다른 단순화).
7. **12지신 말 캐릭터** — 이모지(`ZODIAC`), 팀별 색 배지(`TEAM_COLORS`). 홈에서 팀당 말 개수(1~6, 기본 4) 설정 가능.
8. **말판 이벤트** — 1~19번 칸(모서리 제외)에 수동 지정(드롭다운) 또는 자동 채우기(`autoGenerateEvents`, 빈 칸만 채움, 기존 지정은 보존). 전진/후퇴/한 번 더/쉬기/처음으로 7종. **이벤트는 착지 시 1회만 적용되고 연쇄 발동은 없음** (이벤트로 이동한 칸에 또 이벤트가 있어도 무시).
9. **말판 토글 + 이동 확정(완료) 버튼** — 기본적으로 말판은 숨겨져 있고 카메라 뷰어/결과 화면 상단의 "말판 보기" 버튼으로 열고 닫습니다. 이동할 말을 골라야 할 때는 자동으로 열립니다. 말 선택은 클릭 즉시 적용되지 않고 선택 상태(노란 강조)만 바뀌며, "완료" 버튼을 눌러야 `applyMove`가 실행됩니다(`selectedMove` 상태, `sameOption()`으로 비교).

## 다음에 패치하면 좋을 것 (우선순위 순)

### 높음 — 게임성에 직접 영향
- **Playwright 테스트 보수**: 위에서 언급한 것처럼 흐름이 많이 바뀌어서 기존 `*_test.py`가 깨졌을 가능성이 큽니다. 팀 등록 → 던지기 → 말판 열기 → 이동 → 완료까지 포함하는 새 시나리오 테스트 추가 필요.
- **말판 지름길(대각선) 실제 이동 구현**: 지금은 장식만 있고 실제로는 20칸을 다 돌아야 합니다. 정통 윷놀이의 핵심 전략 요소이므로, 모서리에 도착했을 때 "지름길로 갈까요?" 선택지를 `moveOptions`에 추가하는 방식으로 확장 가능. `board.js`의 `DIAGONALS` 좌표는 이미 있으니 그래프(인접 리스트)만 새로 설계하면 됨.
- **정확히 도착해야 완주하는 규칙**: 지금은 20칸을 넘기기만 하면 완주 처리. 오버슈트 시 못 들어가고 대기하는 정통 규칙으로 바꾸려면 `applyMove`의 `settle()` 로직 수정 필요 (막히면 제자리 유지 등 UX 설계 필요).

### 중간 — 편의성/견고성
- **팀별 말 개수 다르게 설정**: 지금은 전역 설정 하나(`pieceCount`)를 모든 팀에 동일 적용. 팀마다 다르게 하려면 팀 객체에 `pieceCount` 필드 추가.
- **게임 종료 후 처리**: 첫 팀이 완주하면 메시지만 뜨고 게임이 계속 진행 가능한 상태로 남음. 승리 화면/재시작 유도 등 명확한 종료 흐름이 있으면 좋음.
- **말판 오버레이 접근성**: `#boardPanel`이 `position:fixed` 오버레이라서 모바일 좁은 화면에서 겹침/스크롤 확인 필요. 실제 여러 기기에서 확인 안 해봄.
- **이벤트 자동 생성 가중치 조정**: 지금은 7종 이벤트가 완전 균등 랜덤. "처음으로"처럼 파급력이 큰 이벤트 비중을 낮추는 등 밸런스 조정 여지 있음.

### 낮음 — 아이디어 단계
- 여러 기기(폰 여러 대)로 팀을 나눠 플레이하는 네트워크 동기화 (현재는 한 기기·한 카메라를 돌려쓰는 로컬 전제).
- 말판 이벤트에 사운드/애니메이션 효과.
- 팀 이름에 맞는 커스텀 색상 선택 UI (지금은 `TEAM_COLORS` 배열 순서대로 자동 배정).

## 설계상 트레이드오프 메모

- **12지신 이모지 vs 커스텀 아트**: 외부 이미지 자산 없이 가볍게 유지하려고 이모지를 선택. 브라우저/OS마다 렌더링이 다를 수 있음 — 실제 "귀여운" 느낌이 기기별로 다를 수 있으니, 더 통일된 룩이 필요하면 SVG 아이콘 세트로 교체 고려.
- **말판 상태는 저장하지 않음**: 팀 이름/점수/말 개수/이벤트 설정은 `localStorage`에 저장되지만, 진행 중인 말 위치는 저장하지 않습니다. 새로고침하면 게임이 초기화됩니다(의도적 단순화 — 필요하면 `yut-board-state-v1` 같은 키로 저장 추가 가능).
- **이벤트 연쇄 없음 / 지름길 장식만**: 둘 다 "무한 루프·과도한 복잡도 방지"를 위한 의도적 단순화입니다. 위 백로그 항목 참고.
